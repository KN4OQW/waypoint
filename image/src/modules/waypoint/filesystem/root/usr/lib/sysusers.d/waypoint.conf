# The group that may talk to the provisioning helper.
#
# It is a group with no user of its own: waypointd is added to it, and the socket
# is owned root:waypoint. The helper refuses to start if this group is missing —
# a helper whose access control silently resolved to "anyone" would be worse than
# one that does not come up.
g waypoint -
