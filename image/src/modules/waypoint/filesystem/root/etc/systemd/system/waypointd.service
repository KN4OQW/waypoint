[Unit]
Description=Waypoint dashboard + supervisor (live)
Documentation=https://github.com/KN4OQW/waypoint
# Deliberately NOT ordered after network-online.target. On an unprovisioned node
# there is no network to wait for — that is the whole reason the setup access
# point exists — so NetworkManager-wait-online blocks until it times out and the
# daemon that would have raised the access point is what is being delayed. The
# daemon needs no network to start: it binds 0.0.0.0, and its MQTT client
# reconnects on its own.
After=mosquitto.service waypoint-firstboot.service
Wants=mosquitto.service

[Service]
# RFC-0014 power-loss safety: an update that was swapped but never confirmed is
# reverted here, BEFORE the daemon starts, so the unit then runs the prior binary.
# Non-fatal (leading '-') — a boot-check hiccup must never wedge startup. The
# paths are the updater's own defaults (Waypoint home = /home/pi-star/waypoint).
ExecStartPre=-/home/pi-star/waypoint/bin/waypointd -update-boot-check \
  -update-binary /home/pi-star/waypoint/bin/waypointd \
  -update-marker /home/pi-star/waypoint/update.marker
# First start mints the self-signed TLS cert (RFC-0012) and enters the claim flow
# (RFC-0002) — the image ships neither, so the box is unclaimed until an operator
# claims it over the web UI. -release-pubkey is the RFC-0013 key the updater
# verifies waypointd releases against.
#
# The MQTT flags this line used to carry (-mqtt-broker 127.0.0.1:1883 -mqtt-name
# mmdvm) are GONE on purpose. The data plane is store-owned now (#29, the System
# tab), and an explicitly-set flag beats the store by design — so pinning the
# stock values here would have made the broker and topic-root fields inert on
# every packaged node and logged a shadowing warning on every boot. The values
# they set are the daemon's compiled defaults, so removing them changes nothing
# about how a node starts; it only lets the operator's edit take effect.
# -addr stays: the listen address is deployment-owned and shown read-only.
ExecStart=/home/pi-star/waypoint/bin/waypointd \
  -addr 0.0.0.0:443 -tls -http-redirect-addr 0.0.0.0:80 \
  -release-pubkey /usr/share/waypoint/waypoint-release.pub
Restart=on-failure
RestartSec=3
User=root

# The watchdog catches the failure Restart= cannot: a daemon that has deadlocked
# still holds its listener open and never exits, so a hotspot can be off the air
# indefinitely with systemd believing it healthy. waypointd pings from a loop that
# is gated on the event hub answering, so a ping proves the daemon is working
# rather than merely scheduled. 60s is generous for a Pi Zero under a full
# rendering pass; the daemon pings at half that.
#
# Type=notify rather than simple: the daemon signals READY=1 once its listeners
# are bound, so units ordered after it are not started against a socket that is
# not accepting yet.
Type=notify
NotifyAccess=main
WatchdogSec=60s

[Install]
WantedBy=multi-user.target
