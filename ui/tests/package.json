{
  "name": "waypoint-ui-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Dashboard JS unit tests, run with the Node built-in test runner (no framework).",
  "scripts": {
    "test": "node --test"
  }
}
